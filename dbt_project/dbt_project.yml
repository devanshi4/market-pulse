
name: market_pulse
version: '1.0.0'
config-version: 2

profile: market_pulse

model-paths: ["models"]
test-paths:  ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  market_pulse:
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold